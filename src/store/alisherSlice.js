import { createSlice } from '@reduxjs/toolkit'

const initialState = {
   alisher: 'alisher',
}

export const alisherSlice = createSlice({
   name: 'alisher',
   initialState,
   reducers: {},
})
